<h1><%= t('.header') %></h1>

<p>
  <%= link_to 'New test', new_admin_test_path %>
</p>

<table>
  <tr>
    <th>Title</th>
    <th>Level</th>
    <th>Questions</th>
    <th colspain="3">Actions</th>
    </tr>

    <%= render @tests %>
</table>

<ul>
  <% @tests.each do |test| %>
    <li><%= test.title %> (<%= "#{test.questions.size}" %>)</li>
  <% end %>
</ul>
