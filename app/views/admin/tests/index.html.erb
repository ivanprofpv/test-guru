<h1><%= t('.header') %></h1>

<p>
  <%= link_to t('.new'), new_admin_test_path %>
</p>

<table>
  <tr>
    <th><%= t('.title') %></th>
    <th><%= t('.level') %></th>
    <th><%= t('.questions') %></th>
    <th colspain="3"><%= t('.actions') %></th>
    </tr>

    <%= render @tests %>
</table>

<ul>
  <% @tests.each do |test| %>
    <li><%= test.title %> (<%= "#{test.questions.size}" %>)</li>
  <% end %>
</ul>
