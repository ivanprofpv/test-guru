<h1><%= t('.header') %></h1>

<table>
  <tr>
    <th><%= t('.title') %></th>
    <th><%= t('.level') %></th>
    <th><%= t('.questions') %></th>
    <th><%= t('.actions') %></th>
    </tr>

    <%= render @tests %>
</table>

<ul>
  <% @tests.each do |test| %>
    <li><%= test.title %> (<%= "#{test.questions.size}" %>)</li>
  <% end %>
</ul>
