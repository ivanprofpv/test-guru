<h1>Tricky tests</h1>

<p>
  <%= link_to 'New test', new_test_path %>
</p>

<table>
  <tr>
    <th>Title</th>
    <th>Level</th>
    <th>Questions</th>
    <th colspain="4">Actions</th>
    </tr>

    <%= render @test %>
</table>

<ul>
  <% @tests.each do |test| %>
    <li><%= test.title %> (<%= "#{test.questions.size}" %>)</li>
  <% end %>
</ul>
