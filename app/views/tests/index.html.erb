<h1><%= t('tests.index.header') %></h1>

<table>
  <tr>
    <th>Title</th>
    <th>Level</th>
    <th>Questions</th>
    <th>Actions</th>
    </tr>

    <%= render @tests %>
</table>

<ul>
  <% @tests.each do |test| %>
    <li><%= test.title %> (<%= "#{test.questions.size}" %>)</li>
  <% end %>
</ul>
